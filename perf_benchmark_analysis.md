# BPF 挂载机制性能基准分析
## 1. 文档目标
这份文档配合 `darren/practice/perf_benchmark/` 示例使用。
目标是解释三类 BPF 挂载机制的理论差异、实验设计、结果解读方式，以及会影响结果的关键因素。
被比较的对象只有三种：
- kprobe
- fentry
- tracepoint
为了让差异更集中地落在“挂载机制”上，示例里的 BPF 程序只做最小化 per-CPU 计数。
真正的性能比较由用户态通过相同工作负载的总耗时计算完成。
## 2. 为什么这个问题重要
很多 BPF 程序在实验环境里看起来都能正常工作。
但进入生产环境以后，真正关键的问题通常不是“能不能挂上去”，而是“挂上去以后要付出多少成本”。
如果探针位于高频函数上，那么哪怕每次只多几十纳秒，累计到每秒百万级调用时也会变得非常可观。
因此，理解不同 attach 机制的成本差异，是写观测类 BPF 程序的基础能力。
## 3. 三种机制分别代表什么
### 3.1 kprobe
kprobe 是最传统、也最通用的动态函数探针。
它的优势是覆盖面广。
很多普通内核函数都可以直接挂 kprobe。
这让它在老内核和缺少 BTF 的环境里依然非常有价值。
但它的缺点也很明显。
它走的是动态探针路径，进入探针框架的过程相对更重。
在高频热点函数上，这种额外成本往往最容易被放大。
### 3.2 fentry
fentry 是现代内核上非常重要的一类 attach 方式。
它依赖 BTF，使用 BPF trampoline 技术，在函数入口直接进入 BPF 程序。
相比 kprobe，它通常具有更短的调用路径。
它还带来一个额外好处：函数参数的获取方式更自然，代码可读性往往更好。
但 fentry 需要内核和发行版具备相应能力。
如果内核不支持 trampoline 或目标函数缺少合适的 BTF 信息，这一项就会失败。
### 3.3 tracepoint
tracepoint 是静态埋点机制。
它不是“任意函数都能挂”，而是只能挂在内核开发者提前定义好的观察点上。
它的优点是语义稳定、ABI 稳定，而且实现路径通常比较轻。
它的缺点是灵活性不如 kprobe。
如果你关心的内核路径没有对应 tracepoint，就不能直接使用这种方式。
## 4. 本实验为什么选择 open 相关路径
实验使用反复 `openat()` 同一个已有文件的方式作为统一工作负载。
这样设计有几个原因。
第一，它足够简单，容易重复。
第二，`openat()` 这条路径稳定且常见，便于在不同机器上复现实验。
第三，它可以同时触发两个层次的观察点：
- `do_sys_openat2` 这样的内核函数
- `syscalls/sys_enter_openat` 这样的静态 tracepoint
因此，非常适合拿来对比函数级探针与 syscall tracepoint。
## 5. 为什么不在 BPF 侧直接测 delta
理论上，可以在入口与出口分别调用 `bpf_ktime_get_ns()`，然后用哈希表暂存开始时间，最后求出 delta。
但如果实验目的只是比较 attach 机制本身，这么做会引入额外噪声。
这些噪声包括：
- 额外的 helper 调用成本
- 额外的 map 查找和更新成本
- 入口与出口配对逻辑的成本
- 不同挂载点在返回路径上的差异
换句话说，这样测出来的不只是 attach 机制差异，还包含了“测量动作本身”的成本。
所以本实验刻意把 BPF 程序缩到只做计数。
用户态只看相同工作负载在不同 attach 条件下的总耗时。
这种方法虽然简单，但更适合做“整体附加开销”的对比。
## 6. BPF 侧为什么使用 per-CPU array
示例里使用的是 `BPF_MAP_TYPE_PERCPU_ARRAY`。
原因不是为了复杂功能，而是为了避免把全局同步成本混进实验。
如果多个 CPU 同时更新一个共享计数器，那么原子操作、cache line 争用、内存一致性开销都会被放大。
那样一来，你测到的就不再主要是 attach 机制差异，而是 map 更新策略差异。
使用 per-CPU array 后，每个 CPU 都更新自己的局部副本。
用户态最后再把所有 CPU 的值加总即可。
这样更符合“最小化 BPF 程序干扰”的实验原则。
## 7. kprobe 的实现路径为什么更重
kprobe 本质上属于动态探针。
传统实现里，它常常与断点机制联系在一起，例如 `int3` 陷入。
现代内核会做额外优化，例如 jump optimization，但整体上它仍然属于“进入通用探针框架，再进入具体处理逻辑”的模型。
从成本角度看，这条路径往往包含：
- 命中探针点
- 进入 kprobe 框架
- 准备上下文
- 调用 BPF 程序
- 恢复并返回原始执行流
这条链路的灵活性非常强。
代价就是比更专用的 attach 方式多出一些通用层处理成本。
因此，在高频函数上，kprobe 往往不是最低开销的选择。
## 8. fentry 为什么通常更轻
fentry 建立在 BTF 和 trampoline 之上。
它的思想不是通过通用断点框架“截获执行流”，而是在目标函数入口处通过更直接的方式跳到 BPF 程序。
这使得它经常具备两个优势：
- 进入路径更短
- 参数传递更自然
在很多实际测试里，fentry 都比 kprobe 更接近“轻量级函数观测”的理想状态。
这也是现代 BPF 观测工具越来越偏向 fentry/fexit 的重要原因。
不过它并不是无条件可用。
内核必须支持相关能力，目标函数也必须具备合适的 BTF 信息。
## 9. tracepoint 为什么常被视为低开销方案
tracepoint 是内核开发者提前埋好的静态观察点。
它通常和 static key、jump label 一类机制结合使用。
没有启用时，附加成本很低。
启用后，也通常比通用动态探针路径更直接。
因此，很多资料会把 tracepoint 视为低开销、稳定性好的首选方案。
但这里要注意一个前提：tracepoint 只适合“已有埋点”的场景。
如果你需要观察的具体函数没有对应 tracepoint，那么它就不是可选项。
所以 tracepoint 的核心优势是“轻”和“稳”，不是“最灵活”。
## 10. 为什么 tracepoint 不一定永远赢
很多人会把“tracepoint 最低开销”理解为放之四海而皆准的结论。
这其实过于绝对。
在本实验里，tracepoint 挂在 syscall 层，而 fentry 挂在更具体的内核函数层。
两者的命中时机不同，参数准备方式也不同。
因此，即使 tracepoint 通常非常轻，也不意味着它在每个实验里都会明显优于 fentry。
更现实的结论应该是：
- tracepoint 常常很低开销
- fentry 在函数级观测里常常也非常低开销
- kprobe 往往更重一些
## 11. 如何理解输出表格
程序会打印如下维度：
- 总耗时（毫秒）
- 每次调用新增开销（纳秒）
- 相对基线倍率
- 命中次数
其中基线是“无 BPF”场景。
计算公式很直接：
`每次开销(ns) = (当前总耗时 - 基线总耗时) / 调用次数`
这里的“每次开销”是平均值。
它并不代表每次调用都严格多出这个数值。
它只是把总差值平均摊到了每次操作上。
如果某一轮出现轻微负值，不应直接理解为“挂了 BPF 反而更快”。
更常见的原因是调度抖动、缓存状态变化、CPU 频率波动等测量噪声。
## 12. 命中次数有什么意义
BPF 程序除了增加少量计数，不做其他事情。
因此命中次数主要用于验证实验是否真的走到了目标路径。
理想情况下：
- kprobe 命中次数应接近循环次数
- fentry 命中次数应接近循环次数
- tracepoint 命中次数应接近循环次数
如果某一项明显偏低，需要优先怀疑：
- 挂载点没有真正命中
- 目标路径和预期不一致
- 工作负载没有按预想调用对应 syscall 或函数
## 13. 为什么 fentry 要支持“优雅跳过”
fentry 的依赖条件比 kprobe 更强。
如果当前内核不支持 BTF trampoline，或者目标函数不适合做 fentry，加载或附加就会失败。
这种失败并不一定意味着示例代码写错了。
它更可能只是说明当前平台缺少能力。
因此，benchmark 在这类场景下应该给出“跳过”结果，而不是直接终止整个流程。
这样做更适合教学，也更适合跨内核版本测试。
## 14. tracepoint 不存在时应该怎么理解
tracepoint 依赖内核已经定义好的静态埋点。
如果 `syscalls/sys_enter_openat` 不存在，通常表示：
- 内核版本比较特殊
- 当前架构的命名与预期不同
- 内核配置裁剪了相关 trace 设施
这类情况也应被视作“平台不支持该实验项”。
不应简单归类为程序 bug。
## 15. 哪些因素会显著影响结果
实验结果并不是一个放到所有机器上都固定不变的常数。
下面这些因素都会改变结果。
### 15.1 CPU 频率与节能策略
如果 CPU 会动态升降频，那么纳秒级平均值会明显波动。
### 15.2 SMT 与系统噪声
如果同一物理核上的兄弟线程很忙，短路径 syscall 的抖动会更明显。
### 15.3 文件系统缓存状态
虽然示例尽量避免真实磁盘写入，但 VFS、dcache、inode cache 的状态依然会影响 open 路径成本。
### 15.4 其他安全或观测模块
SELinux、audit、LSM、fanotify、其他 tracing 工具，都可能改变 syscall 路径。
### 15.5 内核配置
`CONFIG_KPROBES`、`CONFIG_BPF`、`CONFIG_FTRACE`、`CONFIG_DEBUG_INFO_BTF` 等选项都会影响能力与性能。
### 15.6 编译器与构建参数
用户态循环虽然简单，但不同编译器版本、不同优化选项仍可能略微影响基线。
## 16. 实际使用时怎样选型
如果你的环境较新，而且目标函数有 BTF 支持，那么函数级观测优先考虑 fentry 往往是合理的。
如果你更看重稳定 ABI，且内核已经提供了合适 tracepoint，那么 tracepoint 往往是非常稳妥的选择。
如果你的目标点没有 tracepoint，或者内核较老、BTF 条件不足，那么 kprobe 仍然是最通用的后备方案。
也就是说，三者不是简单的“谁淘汰谁”。
更准确的理解是：
- kprobe 代表通用性
- fentry 代表现代函数级低开销方案
- tracepoint 代表静态埋点型稳定方案
## 17. 本实验的局限性
这份 benchmark 主要用于教学和方向性判断。
它仍然有明显局限：
- 只覆盖 open 路径
- 只比较最小化计数逻辑
- 没有做多轮统计聚合
- 没有固定 CPU 亲和性
- 没有隔离 NUMA 和系统后台噪声
- 没有比较 raw tracepoint、kretprobe、fexit 等更多 attach 方式
因此，实验结果适合回答“谁大概率更轻”，不适合直接当作绝对容量规划数字。
## 18. 如何把实验做得更严谨
如果要进一步提高可信度，可以继续扩展：
- 每种模式重复 5 到 10 次
- 计算中位数、最小值、P95
- 绑定到固定 CPU
- 在更空闲的机器上运行
- 把 benchmark 进程放到隔离核
- 增加多线程版本，观察争用放大后的结果
- 增加 ringbuf 输出版本，比较“仅计数”和“输出事件”的差异
这些扩展可以让实验从“教学示例”逐步接近“工程评估工具”。
## 19. 总结
这份实验最重要的意义，不是给出一个绝对数字，而是给出一个可复现的比较框架。
在这个框架里：
- kprobe 代表最通用的动态函数探针
- fentry 代表基于 trampoline 的现代函数探针
- tracepoint 代表静态埋点型观测入口
你真正应该带走的结论不是“某一种永远最快”。
更重要的结论是：
- 先理解实现机制
- 再在目标环境亲自测量
- 用自己的内核、CPU、发行版和工作负载做最终判断
这也是 BPF 性能分析里最可靠的方法。
